<h2>New Category</h2>

<% form_for :category, @category, :url => { :action => 'create' } do |f| %>
	<p>
		<label for="category[name]" class="required">Name</label><br />
		<%= f.text_field :name %>
	</p>
	<%= submit_tag 'Create' %>
<% end %>